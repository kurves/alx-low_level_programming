#include <stdio.h>
#include "main.h"
/**
 * print_alphabet - check the code
 *
 * Return: Nothing.
 */
void print_alphabet(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
		putchar(i);
	putchar('\n');
}
